@model IEnumerable<Facturacion.Web.Models.SupplierType>

@{
    ViewData["Title"] = "Index";
}

<h2>Tipos de suplidores</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.SupplierTypeId">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.SupplierTypeId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.SupplierTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
