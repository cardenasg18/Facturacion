@model IEnumerable<Facturacion.Web.Models.SupplierType>

@{
    ViewData["Title"] = "Index";
}

<h2>Tipos de suplidores</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Agregar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.SupplierTypeId">Editar</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.SupplierTypeId">Detalle</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.SupplierTypeId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
