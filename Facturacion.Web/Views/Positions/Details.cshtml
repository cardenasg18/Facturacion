@model Facturacion.Web.Models.Position

@{
    ViewData["Title"] = "Details";
}

<h2>Detalles</h2>

<div>
    <h4>Puesto</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PositionName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.PositionName)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.PositionId">Editar</a> |
    <a class="btn btn-dark" asp-action="Index">Regresar</a>
</div>
