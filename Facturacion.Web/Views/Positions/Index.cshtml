@model IEnumerable<Facturacion.Web.Models.Position>

@{
    ViewData["Title"] = "Index";
}

<h2>Puestos de trabajo</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Agregar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PositionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionName)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.PositionId">Editar</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.PositionId">Detalle</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.PositionId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
