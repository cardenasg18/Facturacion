@model IEnumerable<Facturacion.Web.Models.Country>

@{
    ViewData["Title"] = "Index";
}

<h2>Países</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Agregar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountryName)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.CountryId">Editar</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.CountryId">Detalle</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.CountryId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
