@model IEnumerable<Facturacion.Web.Models.Exchange>

@{
    ViewData["Title"] = "Index";
}

<h2>Tasas de cambios</h2>

<p>
    <a class="btn btn-success" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Rate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rate)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.ExchangeId">Edit</a> |
                <a class="btn btn-info" asp-action="Details" asp-route-id="@item.ExchangeId">Details</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.ExchangeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
