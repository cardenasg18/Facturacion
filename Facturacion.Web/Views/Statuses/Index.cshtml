@model IEnumerable<Facturacion.Web.Models.Status>

@{
    ViewData["Title"] = "Index";
}

    <h2>
        Estatus <a class="btn btn-success" asp-action="Create">Agregar</a>
    </h2>

<p>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StatusOf)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatusOf)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.StatusId">Editar</a> |
                <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.StatusId">Detalles</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.StatusId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
