@model Facturacion.Web.Models.Status

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Estado</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StatusOf)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StatusOf)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.StatusId">Edit</a> |
    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>
